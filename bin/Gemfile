# Gemfile
source 'https://rubygems.org'

gem 'clipboard'  # For reading from the clipboard
gem 'json'       # For JSON processing (used in metadata saving)
gem 'fileutils'  # For file and directory operations
gem 'logger'     # For logging (part of Ruby standard library, but listed for clarity)

# If you're using any specific version, you can specify it like this:
# gem 'clipboard', '~> 1.3'

# Add any other gems your project might depend on, for example:
# gem 'nokogiri', if you're parsing HTML/XML
# gem 'rubyzip', if you're working with zip files